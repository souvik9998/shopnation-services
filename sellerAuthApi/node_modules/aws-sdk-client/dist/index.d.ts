import Client from './classes/client';
declare const client: {
    request: (service: string, method: string, params: {}, region?: string, isDebug?: boolean) => Promise<any>;
    Client: typeof Client;
    Service: typeof Client;
};
export default client;
