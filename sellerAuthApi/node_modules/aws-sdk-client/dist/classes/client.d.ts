declare class Client {
    private aws;
    private debug;
    private region;
    constructor(config?: {
        awsClient?: any;
        region?: string;
        debug?: boolean;
    });
    getAwsServiceClient(service: string, region?: string): any;
    request(service: string, method: string, params: {}, region?: string): Promise<any>;
    trackInfo(data: {}): void;
    trackError(data: {}): void;
}
export default Client;
